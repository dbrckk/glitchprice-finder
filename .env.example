# Optional backend endpoint for a future free-tier scraper/API.
# The frontend works without this value because it uses local mock data by default.
VITE_GLITCHPRICE_API_URL=https://deal-finder-backend-y9wb.onrender.com
