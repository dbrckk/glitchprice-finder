{
  "name": "glitchprice-finder",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run check:conflicts && npm run check:real-feed && npm run typecheck && npm run test && npm run build && npm audit",
    "scan:live": "node scripts/scan-live-deals.mjs",
    "check:conflicts": "node scripts/check-conflicts.mjs",
    "scan:live:public": "node scripts/scan-live-deals.mjs --artifacts-dir=public",
    "check:real-feed": "node scripts/check-real-feed.mjs"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/react": "^18.3.29",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^6.0.2",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.30",
    "tailwindcss": "^3.3.3",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vitest": "^4.1.7"
  }
}
